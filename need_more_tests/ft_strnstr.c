include "libft.h"

char	*ft_strnstr(const char *big, const char *little, size_t len)
{
	size_t	big_i;
	size_t	little_i

	big_i = 0;
	little_i = 0;
	if (little[0] == '\0' || little == NULL)
		return ((char *)big);

	while (big_s + 1 - little_s < lem)
	{
		if (big[big_i] == little[little_i])
		{

		}
	}

	
	return (NULL);
}
